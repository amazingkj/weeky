# Weeky Docker Environment Variables
# Copy this file to .env and fill in the values

# Required: 32+ character encryption key for AES-256 (config values encryption)
ENCRYPTION_KEY=change-me-to-a-random-32-char-string!!

# Required: Secret key for JWT token signing
JWT_SECRET=change-me-to-a-random-secret-string!!
